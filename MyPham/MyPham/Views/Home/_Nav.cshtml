@model IEnumerable<MyPham.Models.DanhMucSP>

<div class="flex-col hide-for-medium flex-left">
    <ul class="nav header-nav header-bottom-nav nav-left  nav-uppercase">
        <div id="mega-menu-wrap" class="ot-vm-click">
            <div id="mega-menu-title">
                <i class="icon-menu"></i> Danh mục sản phẩm
            </div>

            <ul id="mega_menu" class="sf-menu sf-vertical">
                @foreach (var item in Model)
                {
                    <li id="menu-item-12690" class="menu-item menu-item-type-taxonomy menu-item-object-product_cat menu-item-12690">
                        <a class="categorycustom" href="@Url.Action("XemSanPhamTheoDanhMuc", "SanPham", new { id = item.MaDM })">@item.TenDM</a>
                    </li>
                }
            </ul>
        </div>
    </ul>
</div>

